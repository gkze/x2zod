{
  "name": "tsconfig",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./base.json": "./base.json"
  },
  "scripts": {
    "check": "../../node_modules/.bin/turbo run lint typecheck test --filter=tsconfig",
    "lint": "bun --eval \"void 0\"",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsgo --project tsconfig.json --noEmit"
  }
}
