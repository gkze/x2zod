{
  "name": "@x2zod/tsconfig",
  "version": "0.1.2",
  "description": "Shared TypeScript compiler configuration for x2zod packages.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gkze/x2zod.git",
    "directory": "packages/tsconfig"
  },
  "files": [
    "base.json"
  ],
  "type": "module",
  "exports": {
    "./base.json": "./base.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "check": "bun --no-env-file run --cwd ../.. runtime:check && ../../node_modules/.bin/turbo run lint typecheck test --filter=@x2zod/tsconfig",
    "lint": "bun --no-env-file --cwd ../.. oxlint packages/tsconfig",
    "test": "bun --no-env-file test ./test",
    "typecheck": "tsgo --project tsconfig.json --noEmit"
  }
}
